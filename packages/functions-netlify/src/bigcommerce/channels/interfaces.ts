export interface ChannelsPostRequestPayload {
    external_id: string;
    is_enabled: boolean;
    name: string;
    platform: string;
    type: string;
  }

  export interface ChannelPostRequestPayload {
    external_id: string;
    is_enabled: boolean;
    name: string;
  }